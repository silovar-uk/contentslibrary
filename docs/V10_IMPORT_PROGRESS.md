# v0.10 取込進行表示

データ取込センターでは、次の4段階を常時表示する。

1. ファイル確認
2. ステージング
3. 重複・件数確認
4. 本番反映

進行中は処理済み件数、残り件数、割合、経過時間を表示する。ステージングと検証の間は「本番未変更」を明記し、本番反映中だけ「本番変更中」へ切り替える。

取込履歴には、現在の状態だけでなく次に必要な操作を表示する。既存の取込APIやDB処理は変更せず、v0.9の画面を監視する表示レイヤーとして実装する。
